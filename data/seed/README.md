# Seed: Deskmate

The instructors' worked example, an internal IT helpdesk agent, run through
all five days. Every notebook falls back to these files until the cohort's own
workspace has the artifact.

Hand-authored and never overwritten by `make seed`:

- `pitch/charter.md`
- `corpus/kb/*.md`
- `transcripts/tickets_raw.jsonl`

Everything else here is produced by a notebook. Regenerate with `make seed`.
